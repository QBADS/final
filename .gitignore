node_modules/
dist/
.DS_Store
*.local
*.tsbuildinfo
.venv/
__pycache__/
*.pyc
services/training-pipeline/registry/
services/middleware/data/

# Real secrets (IBM Quantum API keys, JWT secrets, etc.) must never be
# committed - only .env.example files (checked-in, values always blank)
# document what each service expects.
.env
.env.*
!.env.example
!**/.env.example

# Firebase Admin SDK service account key - a live credential, never commit.
services/middleware/firebase-service-account.json
